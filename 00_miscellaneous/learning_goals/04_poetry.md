### Poetry commands

```shell
# Initialize new project (-n skips setup parameters and sets defaults)
poetry init -n

# Enter peotry virtual environment
poetry shell

# Add dependencies
poetry add fastapi uvicorn